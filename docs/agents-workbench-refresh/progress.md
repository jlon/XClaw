# Agent 工作台重构进度

## 当前状态

- 已完成第一次设计翻新
- 已完成三轮结构与产品动线 review
- 已确认旧的 source list 方案不再作为最终方向
- 已确认新方向是：
  - `我的 Agents` 自适应卡片浏览区
  - `Agent 市场` 卡片墙
  - 右侧 detail workbench
  - 本地 deterministic 头像
- 当前已进入“新版骨架与响应式规则重新锁定阶段”
- 当前重点不再继续报百分比，而是先把新版布局合同与真实实现对齐

## 已完成内容

### 设计与证据层

- 已确认 `workspace` 文件与 `agentDir` runtime 文件必须继续分开
- 已确认 `QClaw` 可借鉴的是技能管理和设置页的桌面结构，不是现成 Agent 页面
- 已确认本地模式必须是“自适应卡片浏览区 + 右侧 detail workbench”，不能退回单列资源卡
- 已确认市场需要独立布局
- 已确认头像需要本地 deterministic 生成
- 已确认“发现靠卡片，编辑靠 detail workbench”是最终信息架构
- 已确认“我的 Agents / Agent 市场”必须做成两个顶层模式
- 已确认人格文件编辑区不能再被压回窄 pane 或弹窗
- 已验证以下开源头像方案仓库存在：
  - `dicebear`
  - `boring-avatars`
  - `jdenticon`

### 头像能力落地

- 已落地本地 deterministic Agent 头像组件
- 已接入新版本地 Agent 卡片与市场卡片
- 已确认头像 seed 以 `agent.id` 为准，避免名称变更导致视觉漂移
- 已补纯函数测试，锁定同 seed 一致、不同 id 变化、左右镜像规则

### 新版骨架已落地部分

- 已拆出 `我的 Agents / Agent 市场` 模式切换组件
- 已拆出本地 Agent 卡片浏览组件
- 已拆出市场卡片墙组件
- 已把主浏览区接回 `Agents` 页面，不再继续依赖旧 source list 做主浏览
- 已把本地卡片的交互收回“点击卡片选中，右侧 detail pane 深入编辑”
- 已把市场卡片的交互收回“点击卡片查看详情，右侧执行安装”
- 已把右侧 detail pane 的 header 收成 `Hero -> chips -> tabs -> section` 的工作台语法
- 已把主工作台比例从过窄 detail pane 收回到更适合人格文件编辑的宽度
- 已把右侧 detail 头部拆成 `Hero -> Actions -> Tabs` 三层
- 已把右侧 detail 从 `概览 / 人格文件` 扩成 `概览 / 人格文件 / 绑定与运行` 三层，避免频道与 runtime 继续挤在概览里
- 已把人格文件区的创建/上传动作移回编辑工作区头部，不再和左侧文件导航抢注意力
- 已把市场详情收成 `来源 / 命名 / 安装` 三段，不再堆 category 统计 chips
- 已把市场安装动作改成底部稳定 action 区，不再和详情滚动混在 header
- 已给市场浏览区补上真实类别筛选；这条有事实依据，因为内置 seed 当前是 58 个、且已分 category 的模板
- 已把新版浏览区与 detail pane 的新增文案并回 `agents` 多语言包，不再依赖页面硬编码
- 已给本地 Agent 搜索和市场搜索补稳定 `aria-label` 与缺翻译 fallback，避免实现和测试继续分叉
- 已把本地 detail pane 的重复信息压平：
  - `当前 Agent` 只在 hero 说一次
  - overview 改成 `核心摘要`
  - 右侧动作区改成 `快捷操作`
- 已确认新版 `agents-workbench-layout` 定向回归重新全绿，当前 `6/6`
- 已把 `Agents` 页头从“大标题 + 描述 + 动作”收成轻工具条，模式切换只负责切模式，不再承担说明文案
- 已把 `我的 Agents` 浏览区改回“搜索 + 卡片浏览区”，不再在浏览区顶层重复塞安装入口
- 已把本地 Agent 卡片收成 `身份层 + 微元信息层`：
  - 主体只保留头像、名称、id、默认/继承标记
  - 当前模型与绑定频道数量压回轻 meta
  - 去掉把 `workspace` 目录名当角色信息的底栏
- 已把市场模式收成独立的搜索/筛选工具条，不再和全局 `CreateAgentLauncher` 混排
- 已把 `Agents` 页头继续收成“标题 + 模式切换”，创建/安装/刷新动作全部回到当前浏览模式的工具条
- 已把本地 Agent 浏览工具层锁成：
  - 页头只负责模式切换
  - 左侧浏览 rail 负责搜索、刷新、新建、安装来源选择
  - 右侧 detail workbench 负责深入编辑
- 已把右侧本地 Agent 详情继续收成桌面工作区：
  - Hero 只保留 `开始对话` 作为唯一主动作
  - `编辑 / 打开工作区 / 显示工作区 / 删除` 压回次级 utility strip
  - `概览 / 人格文件 / 绑定与运行` 三块继续退出后台 inspector 语法
- 已把市场卡片继续收成“摘要卡”，不再把亮点、安装方式、来源路径同时塞进卡片主视觉
- 已把市场详情收成：
  - 上层模板摘要 + 安装块
  - 下层 `核心亮点 / 模板详情 / 来源证据`
  - 安装动作保持优先，来源说明降为核对层
- 已把市场详情区压成“来源理解 + 安装动作”两段，去掉重复的来源说明、分类/installMode 重复 badge 和后台式路径堆叠
- 已把本地 detail pane 的 hero 和 overview 再去后台化一轮：
  - hero 小标题不再走全大写后台标签
  - overview 不再重复暴露 `runtime state / agentDir`
  - `当前模型` 卡不再把 `mainSessionKey` 这类内部会话键暴露给用户
- 已确认当前内置市场 catalog 为 `58` 个模板，并补上了缺失的 `management` 分类与更完整的 `development` 宽度
- 已把本地 Agent 卡片继续从“统计卡”收回“选择卡”：
  - 主体不再优先暴露 `agent.id`
  - 模型与频道绑定压成一条次级摘要与轻 meta
- 已继续压掉本地模式里重复的后台信息：
  - 本地卡片底部不再重复展示模型，只保留绑定频道轻 meta
  - `概览` 页不再重复用事实卡展示频道数量
  - 频道管理入口已回到频道摘要卡头部，而不是继续埋进后台式快捷操作区
- 已把切换本地 Agent 的行为改成“保留当前工作模式”，不会再一律把右侧送回 `概览`
- 已把市场浏览继续从“目录”收成“商店”：
  - 市场卡片不再按名字字母序强制重排，优先保留 catalog 顺序
  - 卡片去掉 `sourcePath` 暴露，只保留角色卖点和可安装语义
  - 分类过滤压成主分类 chips + 次级分类下拉，不再把完整 taxonomy 顶到首屏
- 已继续把市场卡片墙压成“摘要卡 + 安装详情”语法：
  - 卡片主视觉只保留名称、分类、可安装状态、一条摘要和一条主亮点
  - 来源路径退到卡片底部次级位，不再与分类、安装状态抢首屏
  - 右侧详情继续保持“安装优先，来源核对其次”，安装方式从 hero badge 降回安装语义提示
- 已把市场详情顺序改成“安装动作优先，来源信息次之”，避免 detail pane 首屏像模板 inspector
- 已把市场模式的外层工作台提前到默认桌面宽度进入双栏，市场不再等到 `xl` 才常驻 detail pane
- 已把市场筛选工具条收成 sticky pane chrome，而不是普通页面内容
- 已把市场详情从“右栏里的右栏”改成单一 detail pane：
  - 模板摘要与安装块继续留在顶部
  - `来源 / 安装模式 / rawUrl` 降成紧凑元信息
  - 下层只保留 `核心亮点 / 模板详情 / 来源证据` 的单列流
- 已把市场卡片继续从“网页模板卡”收回“桌面商店摘要卡”：
  - 去掉顶部光斑和角落渐变
  - 不再同时堆叠摘要与亮点
  - 底部优先展示 `可安装 / 安装模式 / 来源叶子名`
- 已把本地 detail hero 的动作密度继续压回高频工作流：频道跳转只留在 `绑定与运行`，hero 不再重复暴露同一个导航动作
- 已根据最新产品判断纠正一个旧前提：本地 `我的 Agents` 不再收回单列资源卡，而是改成自适应卡片浏览区；市场继续保留卡片墙
- 已锁定本地模式的核心响应式规则：
  - 标准窗口优先 `2` 列
  - 更宽窗口可到 `3` 列
  - 较窄窗口退回 `1` 列
  - detail workbench 在标准窗口下继续常驻
- 已把右侧 detail pane 的第一主动作改成“开始对话”，页面重心从“维护记录”转回“使用 Agent”
- 已把市场 detail pane 收成“安装优先，来源核对其次”，左侧市场卡片不再暴露 `sourcePath/rawUrl`
- 已把内置市场 seed 升级成富元数据 catalogue：
  - 每个模板都带 `localeKey / avatarSeed / headline / summary / highlights / detailSections / tags`
  - 摘要与详情来自上游真实 `SOUL.md` 解析，不再只靠 `role/sourcePath`
- 已补市场内容解析层：
  - 卡片摘要优先显示 `summary`
  - 详情页新增 `核心亮点 / 模板详情`
  - 搜索已覆盖 `headline / summary / highlights / detailSections`
- 已把国际化支持收成“壳层文案 + 章节标题本地化 + 内容层可信回退”：
  - `zh / en / ja` 已覆盖搜索、安装、分组、章节标题等 UI shell
  - 当模板本身没有人工校对过的本地化内容时，继续保留上游源语言
- 已把本地模式回归断言收口到“自适应卡片浏览区 + 右侧 detail workbench + 正确搜索壳层文案”，避免旧 `Agent` / `智能体` 文案继续漂移
- 已修正市场同步脚本的两个真实问题：
  - 少数模板 `headline/summary` 退回 slug 的生成缺陷
  - Node `fetch` 对 GitHub raw 超时的脚本脆弱性，现已改为系统 `curl` + 重试
- 已把本地 detail workbench 的 `概览` 再收成“左侧主事实 + 右侧 utility / runtime”，不再卡片套卡片
- 已把市场卡片墙去掉顶部目录 banner，并在 `headline === name` 时抑制重复副标题
- 已把本地智能体卡片继续去重：
  - 去掉重复的模型 chip
  - 只保留名称、状态 badge 与绑定频道轻 meta
- 已把本地智能体卡片继续从“统计卡”收成“选择卡”：
  - 去掉底部重复频道 pill
  - 名称下只保留一条轻摘要，不再底部再起一层状态条
- 已把本地详情继续退出后台 inspector：
  - hero 只保留身份、轻摘要与唯一主动作“开始对话”
  - `编辑 / 打开工作区 / 显示工作区 / 删除` 已下沉成次级 utility strip
  - `概览` 不再重复展示 runtime/path 事实卡
- 已把人格文件编辑区继续收成主工作区：
  - 文件工具条与保存动作已经并回编辑器顶部 chrome
  - 编辑器成为主视觉，辅助说明降到次级
- 已把市场卡片继续收成“商店摘要卡”：
  - 底部不再优先暴露来源路径
  - 第三条信息改成亮点摘要，继续退出目录/后台语法
- 已继续删除市场模式里的重复说明层：
  - 卡片不再重复解释“可安装 / 安装方式”
  - 右侧详情不再给每个区块追加说明句
  - 安装区与来源证据彻底分层
- 已把市场详情继续收成“理解 → 安装 → 证据”：
  - hero 只保留类别、名称、headline、summary
  - 安装块抬成唯一主任务面，`可安装 / 安装模式` 并回安装区
  - `来源证据` 单独下沉为最后一段，不再与 hero/安装块抢首屏
- 已完成一轮最终节奏收口：
  - 页头继续压紧，模式切换更接近桌面 segmented control
  - 本地卡片浏览区与详情 pane 的内边距和高度节奏继续统一
  - 本地 detail hero 的 utility strip 继续缩紧，不再像后台动作盒
- 已按默认窗口的真实观感纠正本地模式布局：
  - 默认桌面宽度下，本地浏览区回到单列卡片
  - 右侧 detail workbench 明显增宽，不再挤压 hero 与概览事实
  - `概览` 事实卡不再强制三列，避免文字错位
- 已完成最后一轮审美收口：
  - 本地卡片选中态回到更像桌面选择卡的边界与阴影
  - 本地详情 hero 改成“身份 + 主动作 + 轻事实网格”，不再把长摘要和模型 chip 硬塞在一层
  - 人格文件区的文件列表与编辑器 chrome 已进一步做成编辑器工作区
  - 市场卡片墙与详情区继续压平后台味，卡片更统一，安装区和证据区层级更清楚
- 已修正本地 Agent 卡片的核心违约点：移除 `auto-rows-fr + min-h` 的等高白板布局，卡片回到按内容自然高度的资源板骨架，不再在卡片正文中保留大片空白槽位

### 已有实现资产

以下实现不会被推翻，只会被迁移到新布局中：

- Agent CRUD
- `workspace` 文件接口
- 市场 catalogue 与安装链
- runtime refresh / rollback
- 脏状态与 `Cmd/Ctrl+S`

## 当前判断

### 当前判断

这轮 review 后，文档不再继续使用 `92%-96%` 这类没有约束力的进度估算。  
当前更准确的表述是：

- 设计红线：已锁死
- 本地模式的自适应卡片浏览区：主结构已落地，剩余是卡片密度与默认窗口下的观感收口
- 市场浏览与安装布局：主结构已闭环，剩余是桌面商店陈列感收口
- 右侧 detail pane：已进入收尾阶段
- 剩余主要是最终手工体验复核与少量层级精修
- 创建入口文案已收成桌面工具栏语法：主按钮使用短动词，分流语义留给下拉菜单，弹窗标题保留完整名词
- `概览` 已移除没有独立价值的“核心摘要”外壳，事实卡直接成为第一层信息，减少后台感与重复层级
- `概览` 中重复的人格文件/工作区事实卡已删除，默认只保留真正需要在概览承接的频道归属
- `概览` 页签已退役；本地智能体 detail 现在只保留两个真实工作面：`人格文件` 与 `绑定与运行`
- `绑定与运行` 已改成“频道账号主任务面 + 运行事实次级摘要”，不再把运行状态和运行时目录抬成与频道管理同级的后台 inspector
- `人格文件` 已收口成“内置文件清单 + 弹窗编辑”，默认不再暴露创建、上传、重命名、删除这套文件管理器分支
- 智能体设置弹窗已按工作台质感合同收成“名称 + 轻只读 ID + 模型主编辑位”，删除了头部说明和模型常驻解释文案，不再把只读事实与主编辑任务做成同重量大卡
- 人格文件内置文件列表已补上轻微 hover 动效，只保留桌面控件级反馈：轻抬升、边界增强和短阴影，不引入网页卡片漂浮感
- 已给“编辑智能体”补上真实模型覆盖能力：
  - 设置弹窗现在支持搜索可用模型
  - 支持回退为 `OpenClaw` 默认模型
  - 保存会持久化到 `openclaw.json`
  - 只有模型变更才触发后台异步 runtime restart
  - 名称单独修改不会触发不必要的 runtime 重启
- 已把市场安装模板固化进仓库：
  - 安装时优先读取内置 `SOUL.md` 模板资产
  - 不再依赖 GitHub raw 网络拉取
  - 代理、超时、离线状态不再成为市场安装主链路单点
- 已把创建/市场安装链改成原子化提交：
  - 智能体创建成功后，如果 provider sync 或 runtime 应用失败，会自动回滚配置与受管 workspace
  - 市场安装成功后，如果后置 provider sync 或 runtime 应用失败，同样会自动回滚
  - 不再出现“接口报失败，但 openclaw.json 和磁盘目录已经成功创建”的分叉状态
- 已把删除链补齐到同一标准：
  - 删除成功前不再提前清理 runtime 目录
  - `replaceRuntime()` 失败时会自动恢复删除前的 `openclaw.json`
  - 只有 runtime replacement 成功后，才会继续删除 runtime 目录与 workspace
  - 不再出现“接口报失败，但智能体配置已删掉”的假失败
- 已把创建/市场安装的 runtime 契约补平：
  - 当 Gateway 当前是 `stopped` 时，创建/安装不会再静默跳过 runtime 应用
  - 现在会先确保 runtime 启动，再按场景决定是否 restart
  - 目标是让新智能体在 OpenClaw 里真实可用，而不是只写入配置
- 已把市场安装前端链路补平：
  - 安装成功后直接消费 POST 返回的 `snapshot`
  - 不再额外依赖一次 `fetchAgents()` 自证成功
  - 避免“POST 已成功但后续 GET 失败，UI 误报安装失败”的假失败
- 已把非主智能体的工作区文件列表收回真实差异文件：
  - 市场安装与空白创建默认不再克隆 `main` 的整套人格 bootstrap
  - 文件列表会隐藏与 `main` 完全一致的继承 bootstrap 文件
  - 已安装的 `thumbnail-designer` 这类智能体不再在 UI 中伪装成还在编辑 `main` 的人格文件
- 已收掉智能体设置弹窗模型选择器的外露滚动条：
  - 外层 modal 不再因为内容区 `overflow-y-auto` 暴露浏览器滚动条
  - 内层模型列表改用现有 subtle scrollbar 语法，避免破坏工作台铁律
- 已把 `Agent 市场` 的中文内容从“壳层翻译兜底”升级成独立内容资产链：
  - 中文环境下，卡片摘要与详情章节优先读取受控中文内容资产
  - 英文/日文继续安全回退到 seed 原始内容
  - 不再出现“市场卡片与详情都还是英文”的半本地化状态
- 已按 `WU-01 / WU-02 / WU-04 / WU-07` 继续收剩余后台感：
  - `编辑智能体` 弹窗移除了只读频道列表，只保留 `名称 / 智能体 ID / 模型`
  - hero 下的两张事实卡退成轻事实带，只表达 `当前模型 / 频道绑定`
  - `内置文件` 列表去掉每行重复的 `内置` badge，回到单行资源列表
  - `Agent 市场` 详情不再拆成三张等权重大卡，已并成“安装主面 + 连续 detail pane + 最后一段来源证据”
  - 模型设置弹窗的 `ID / 模型` 已压成同一节奏的紧凑编辑面，不再出现“大而空”的网页式表单壳
- 已补齐“新建智能体”时的模型供应商/模型选择能力：
  - 新建弹窗不再只有名称输入，已复用与聊天/设置一致的模型选择器
  - 选中值持久化为完整 `modelRef`，格式是 `runtimeProviderKey/modelId`
  - 默认仍可继承 OpenClaw 默认模型，不强制每次显式覆盖
  - `POST /api/agents`、store、`createAgentWithId()` 已统一支持 `modelRef`
  - 新建后会像现有创建链一样走 provider sync 和 runtime 应用，不会只改 UI 不改底层

## 下一步

1. 继续收本地卡片墙在默认窗口下的密度与 hover 语法
2. 继续收市场卡片墙与右侧安装详情的“桌面商店”层级
3. 继续压平人格文件工作区工具条，让编辑器成为唯一主任务面
4. 锁死市场安装后的回跳、高亮与 detail pane 同步 UX
5. 决定新版稳定后是否退役旧 `AgentListPane`
6. 决定是否为高频模板补人工校对过的 `zh / ja` 内容覆盖，而不是继续完全依赖源语言回退
7. 补 Linux smoke 验收项，完成真实窗口手工 review

## 暂不再沿用的旧方向

- 左侧 source list 作为主浏览入口
- 市场与本地 Agent 共用同一套窄列表
- 首字母圆章作为最终头像方案
