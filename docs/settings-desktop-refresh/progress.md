## 当前进度

### 已完成

- 建立 `settings-desktop-refresh` feature 文档
- 完成首轮产品 review，确认 `Settings` 当前核心问题是信息架构，而不是局部样式
- 明确第一阶段目标：设置页从长网页改成桌面设置面板
- 完成设置页顶部 tabs + 单激活内容 pane 骨架
- 完成 `通用偏好 / 运行环境 / 更新 / 开发者` 的 section 重排
- `devMode / telemetry` 已从运行环境移回通用偏好，避免语义错位
- `运行环境 / 更新` 已开始按 `card + row` 收口，而不是连续长表单
- `Developer` 区已开始按 `代理 / 命令与控制台 / Doctor / 调试工具` 分组收口
- 已完成 `eslint` 与 `typecheck` 验证，确认首轮结构改造未打断功能链路
- `UpdateSettings` 已并回统一 pane 语法，不再自成大圆角网页卡片
- `Gateway / Developer` 的高频动作已改成“摘要先行、细节后置”的桌面面板节奏
- `Developer` 已恢复 `OpenClaw 控制台` 入口，并调整为“控制台 -> 令牌/CLI -> Doctor/埋点”的桌面 utility 动线
- `Doctor / 埋点查看器` 的摘要已从 badge 条改成小型信息卡，继续向固定 pane 的 `card + row` 语法收口
- `Doctor / 埋点查看器` 的原始输出已改成按需展开，不再默认铺满设置页
- `更新` 面板已退出 `prose/文章式` 语法，改回固定 pane 内的摘要 + 状态 + 发布说明
- 普通输入框已退出全局 monospace，仅 Token / CLI 保留技术值语义
- 更新面板内的英文硬编码已清理，继续收紧桌面设置的一致性
- 代理设置已改成“主代理先行，高级字段按需展开”，不再默认摊成工程后台表单
- 设置内容区已收成固定阅读宽度，继续向 `QClaw` 的固定 pane 语法靠拢
- `运行时` 已改成单一控制台：紧凑事实带 + 工具带 + 代理 pane
- 代理已从 `Developer` 区迁回 `运行时`
- 日志已从内联长块改成独立模态，不再把设置页拉成长网页
- `Developer` 已退出“代理 + 开发工具混层”，只保留真正的开发者诊断能力
- `Developer` 的控制台、令牌/CLI、Doctor、调试工具已收成三个稳定 pane，不再堆成后台长页面
- `Doctor / 埋点` 的摘要已从小指标卡改成紧凑事实带
- `更新` 已退出“大版本卡 + 状态块 + 帮助尾注”的网页语法，改成单层更新 pane
- `通用偏好` 已退出“两张大表单卡 + 三条大 toggle”的网页语法，改成 `主题/语言` pane + `启动/遥测/dev mode` pane
- `appearance.description` 不再被错误复用为字段说明，`主题 / 语言` 已回到桌面 segmented row 语法
- 设置页各 section 头部的泛说明已开始清理，避免“标题再解释一遍”的网页噪音继续拉长页面
- 设置页页头已彻底移除，`设置 / 配置您的 XClaw 体验` 这类重复标题与泛解释文都已删除，只保留顶部 tabs 和当前 pane
- 设置页主区已从 `tabs -> 大 section -> subpanel` 收成 `tabs -> 单一 pane -> 分隔式 rows`
- `更新` 已退出白卡套白卡，改成 pane 内的状态摘要 + 分隔式发布信息
- `应用日志` 模态已退出“标题区 + 独立动作横条 + 大白块”的网页语法，改成单头部工具带 + 内容 pane
- `通用偏好 / 更新` 已引入固定“标签轨 + 控件轨”，控件不再被甩到 pane 最右边制造网页式死空白
- `更新` 与 `调试工具` 中残留的厚 toggle 行已并回统一的紧凑 row 语法，避免一页内出现多套开关风格
- `关于` 已从设置页 tabs 与内容区彻底移除，避免低频信息继续占据主工作区
- 设置页已退出“左导航 + 长滚动 section”结构，改为顶部 tabs 切换单一 pane
- 设置页顶部 `tabs` 与 `主题 / 语言` 切换已退出“整条白壳分段控件”语法，改成更接近桌面工具按钮的独立控件组
- `Settings` 的标签轨与控件轨已继续收窄，utility 按钮也开始统一到同一套桌面按钮质感，减少 pane 中部死空白
- `运行环境 -> 代理` 已继续按 `WU-04` 减字，删除重复说明文，只保留标题、字段和值，不再靠帮助文把 pane 拉成网页表单
- `更新` 在开发环境下已退出红色错误态，改为中性“仅打包版可用”提示，避免把未打包构建误报成更新失败
- OpenRouter 的旧 `claw-x.com / XClaw` 头注入已从 provider 默认配置、Gateway dev preload、独立 preload 三个入口一起清掉
- 设置页主区已进一步收成稳定的 inspector 语法：`标签 -> 值/控件 -> 动作`，不再允许“左边说明文、右边控件”的网页表单排法
- `通用 / 网关 / 更新 / 开发者` 主路径里的说明性文案已继续退出第一视觉层，只在长输出和次级诊断区保留
- `网关 -> 代理` 已收成两行主路径：`代理 + 开关`、`代理服务器 + 输入 + 动作`，高级字段继续作为次级区按需展开
- `Developer` 的 `控制台 / 网关令牌 / CLI / WS 诊断 / 埋点查看器` 已回到同一套 row 语法，不再通过卡片套卡片制造层级
- `应用日志` 模态头部已删除“最近 100 行”说明，保持单一标题 + 工具动作的桌面模态语法
- `更新` 已改成“当前版本 + 禁用说明”，自动检查更新和自动下载更新开关已移除

### 纠偏

- 已确认前一版假设有误：`AI Providers` 不属于 `Settings` 主体，真实挂在 `Models` 工作台
- 设置页本轮只优化系统设置的信息架构，不搬运 Provider 管理
- 已补充 `QClaw` 证据：其设置使用固定尺寸面板、左菜单、右侧 `card + row`，不是长网页
- 已修正控制台入口的首次点击逻辑：没有缓存 URL 时，刷新后也必须直接打开，而不是要求用户点第二次

### 下一步

- 继续收中小窗口下 `Settings` 的节奏密度，避免 pane 在默认视口里偏松
- 做一轮设置页中小窗口与 tabs 切换验收
