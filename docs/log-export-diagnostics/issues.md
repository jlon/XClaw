# 平台日志导出问题记录

## 当前已知边界

1. 当前导出只包含平台日志，不包含 OpenClaw runtime 文件
2. UI telemetry 仍然是内存态，不会随导出包落盘
3. 如果日志目录为空，导出接口会返回失败

## 后续可选增强

1. 增加“最近 1 份 / 最近 10 份 / 全部日志”的导出范围
2. 增加结构化错误摘要文件
3. 增加可选的 doctor 输出附带能力

