# XClaw 官网测试

## 目标

本文件用于记录官网页面在实现阶段和交付前的验证口径。

本轮官网不是桌面应用功能页，而是本地可预览的单页站点，因此验证重点不是业务状态流，而是：

- 页面是否能稳定打开
- 视觉和结构是否符合设计
- 图片与素材是否全部来自本地仓库
- 桌面端和移动端是否都可正常浏览

## 计划验证项

### 1. 本地预览

- 官网入口可直接启动
- 本地访问地址正常打开
- 刷新页面后不白屏

### 2. 页面结构

- 首屏、能力区、场景区、下载区、FAQ 区顺序正确
- 导航锚点可跳转到正确区块
- 结尾 CTA 不丢失

### 3. 文案术语

- 主标题、副标题、区块标题符合设计文档
- 不混入 `runtime / gateway / plugin / orchestration` 等内部词
- 不把页面写成开发者工具说明书

### 4. 素材加载

- Logo 正常显示
- README 截图正常显示
- 渠道和模型图标不丢失
- 不依赖外链图片

### 5. 响应式

- 常规桌面宽度下布局稳定
- 窄屏下模块正常折叠
- 移动端文本不溢出、按钮可点击

### 6. 交互

- 导航点击可跳锚点
- 截图区域切换正常
- 下载按钮样式与可点击态正常

## 计划执行命令

根据最终落地方式，至少补齐以下验证：

- 官网本地启动命令
- 静态构建命令
- 类型检查命令
- 需要时的 lint 命令

## 当前状态

2026-03-23：

- 已完成设计确认
- 已完成独立 `website/` 官网实现
- 已根据用户反馈将官网结构进一步压缩为更接近下载发布页的形式
- 已根据用户反馈补充首页动效，包括首屏入场、滚动显现、截图切换动画
- 已根据用户反馈将官网进一步重排为更接近完整产品站的结构
- 已根据用户反馈继续向 `youclaw` 的精致感对齐，完成第二轮视觉精修
- 已根据用户反馈吸收 `qclaw` 的下载按钮样式语言
- 已根据用户反馈上提 Hero 主体，收紧导航下方留白
- 已修正 Hero 顶部误带的额外区块间距
- 已按用户新增截图文件名补充官网预览内容
- 已修正官网截图资源在构建产物中的打包方式
- 已移除预览区重复的假窗口顶栏
- 已收紧 Hero 主标题排版
- 已执行 `pnpm run build:vite`
- 已执行 `pnpm run typecheck`
- 已执行 `pnpm exec vite preview --host 127.0.0.1 --port 4173`
- 已执行 `pnpm exec vite preview --host 127.0.0.1 --port 4174`
- 已在浏览器手工验证 `http://127.0.0.1:4173/website/index.html`
- 已在浏览器手工验证 `http://127.0.0.1:4174/website/index.html`
- 已在浏览器对照 `https://youclaw.dev/` 与本地官网当前版本的首屏和场景卡层次
- 已在浏览器对照 `https://qclaw.qq.com/index.html` 与本地官网当前版本的下载按钮比例与信息密度
- 已在浏览器手工确认 Hero 主体在常规桌面宽度下不再明显下坠
- 已在浏览器手工确认导航下方不再出现异常的大面积首屏空白
- 已在浏览器手工确认预览区可切换 `对话工作台 / 模型管理 / 智能体 / 多渠道接入 / 技能扩展 / 定时任务 / 设置`
- 已在浏览器手工确认当前预览截图 `naturalWidth / naturalHeight` 为有效值，不再是空资源
- 已在浏览器手工确认预览区不存在双顶栏重复问题
- 已在浏览器手工确认 Hero 主标题不再被硬拆成 4 行字墙
- 已确认截图切换可在 `对话工作台 -> 多渠道接入` 之间正常切换
- 已确认滚动到中段后 `data-reveal` 区块会按顺序进入可见态
- 本轮按用户要求跳过自动化测试，未执行 `vitest` 与 `Playwright`
