# 聊天欢迎页稳定性问题记录

## 已知问题

1. 启动首屏在 Gateway 未就绪时会渲染运行态文案，新建聊天欢迎页通常不会，导致两者视觉中心不一致。

## 风险

1. 如果直接隐藏运行态文案，会损失首屏状态反馈。
2. 如果让状态文案继续参与普通流布局，高度抖动会持续存在。

## 处理结果

1. 运行态文案改为固定槽位，`running` 时保留占位但不播报内容。
2. `starting` / `error` 等状态仍会在同一位置显示，不再改变欢迎页整体高度。
