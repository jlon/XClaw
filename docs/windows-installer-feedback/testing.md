# Windows 安装器日志与取消交互测试记录

## 计划验证

1. 静态测试确保 `electron-builder` 仍然引用 `scripts/installer.nsh`。
2. 静态测试确保安装器脚本具备以下关键语义：
   - 默认显示安装详情
   - 安装页启用取消按钮
   - 模板执行 `SetDetailsPrint none` 后重新开启详情输出
   - 取消时弹出确认
3. 打包后在 Windows 安装器中人工确认：
   - 进入安装页后能看到实时日志
   - 取消按钮可点击
   - 取消时弹确认

## 本次执行

待实现后补充。
