# 聊天消息区桌面 IM 质感对齐实现计划

## 目标

在不破坏现有富消息能力的前提下，把 XClaw 消息区的布局语义与视觉节奏对齐到 QClaw 通用桌面 IM。

## 任务拆分

1. 先调整 `ChatMessage.tsx` 结构，引入主消息壳层并把元信息从次级内容流中移出。
2. 再调整 `globals.css`，统一用户/助手列宽、气泡半径、正文留白和次级块样式。
3. 同步修正现有源码级测试与 DOM 行为测试。
4. 执行定向 `vitest`、`eslint`、`typecheck`。
