# 聊天左栏品牌位问题记录

## 1. 品牌位最初完全缺失

聊天左栏只有搜索、会话列表和底部工具，没有任何品牌承接。

这会导致：

- 聊天页和工作台页的侧栏语言不一致
- 左栏顶部缺乏识别锚点
- 用户只能在工作台页看到品牌，在聊天页感知断层

处理：

- 在聊天左栏顶部补一个紧凑品牌位

## 2. 不能再造一套聊天页私有品牌结构

如果聊天页直接复制 `Sidebar` 的 `logo + XClaw` 结构，会再次形成两套近似实现。

处理：

- 抽共享品牌组件
- `Sidebar` 和 `ChatSessionsPane` 都改为复用
