# 根目录清理问题记录

## 当前已知问题

1. 根目录存在 `.DS_Store`，虽然已被忽略，但仍会污染本地视图。
2. 根目录保留了两个无引用的 Anthropic 调试脚本，增加判断成本。
3. 构建产物与源码目录并列，缺少统一清理入口。

## 暂不处理

1. `node_modules/` 不纳入脚本清理范围，避免误导为常规操作。
2. `.reference/` 与 `.superpowers/` 为本地参考目录，不参与打包，但也不默认清理。
